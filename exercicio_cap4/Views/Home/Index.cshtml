@model exercicio_cap4.Models.Home

@{
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
    ViewBag.Title = "Listagem de CATEGORIAS";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
        <table class="table">
            <tr>
                <th>
                    Temas
                </th>
            </tr>
            <tr>
                @foreach (var item in Model.Tema)
                {

                    <td>
                        @Html.DisplayFor(modelItem => item.Nome)
                    </td>

                }
            </tr>
        </table>
    </div>
</body>
</html>